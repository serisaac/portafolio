---
const phrases = ['desarrollador frontend', 'amante del buen código', 'apasionado por Astro', 'aprendiendo cada día'];
const codeSnippet = String.raw`const portfolio = {
  stack: 'Astro',
  style: 'cappuccino',
  focus: ['accesibilidad', 'rendimiento']
};`;
---

<section class="hero" id="top">
  <div class="hero__content">
    <p class="eyebrow">Sergio Isaac Moreno Alvarez · Astro 4</p>
    <h1>Construyo interfaces limpias, rápidas y con mucha intención visual.</h1>
    <p class="hero__lede">
      Soy un desarrollador principiante enfocado en fundamentos sólidos, accesibilidad y una experiencia visual que se siente cuidada desde el primer scroll.
    </p>

    <div class="terminal" aria-label="Terminal animada">
      <div class="terminal__header">
        <span></span><span></span><span></span>
      </div>
      <div class="terminal__body">
        <p class="terminal__prompt">usuario@astro:~$</p>
        <p class="terminal__line" aria-live="polite">
          <span class="terminal__cursor">▍</span>
          <span class="terminal__phrase" data-phrases={JSON.stringify(phrases)}>desarrollador frontend</span>
        </p>
        <p class="terminal__hint">Typewriter effect hecho con Vanilla JS puro.</p>
      </div>
    </div>

    <div class="hero__actions">
      <a class="button button--primary" href="#projects">Ver proyectos</a>
      <a class="button button--ghost" href="#contact">Contactar</a>
    </div>
  </div>

  <div class="hero__aside">
    <article class="code-card" aria-label="Tarjeta de código flotante">
      <p class="code-card__title">Snippet destacado</p>
      <pre><code>{codeSnippet}</code></pre>
    </article>

    <div class="scroll-indicator" aria-hidden="true">
      <span>Scroll</span>
      <i></i>
    </div>
  </div>
</section>

<script>
  const phraseNode = document.querySelector('.terminal__phrase');
  if (phraseNode) {
    const phrases = JSON.parse(phraseNode.dataset.phrases || '[]');
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    let index = 0;
    let charIndex = 0;
    let deleting = false;

    if (reduceMotion || !phrases.length) {
      phraseNode.textContent = phrases[0] || phraseNode.textContent;
    } else {
      const tick = () => {
        const current = phrases[index];
        const nextLength = deleting ? charIndex - 1 : charIndex + 1;
        charIndex = Math.max(0, Math.min(current.length, nextLength));
        phraseNode.textContent = current.slice(0, charIndex);

        if (!deleting && charIndex === current.length) {
          deleting = true;
          setTimeout(tick, 1100);
          return;
        }

        if (deleting && charIndex === 0) {
          deleting = false;
          index = (index + 1) % phrases.length;
        }

        const delay = deleting ? 45 : 70;
        setTimeout(tick, delay);
      };

      setTimeout(tick, 500);
    }
  }
</script>

<style>
  .hero {
    max-width: 1160px;
    margin: 0 auto;
    padding: 8.5rem clamp(1rem, 3vw, 2rem) 4rem;
    display: grid;
    gap: 2rem;
    min-height: 100svh;
    align-items: center;
  }

  .hero__content {
    display: grid;
    gap: 1.35rem;
  }

  .eyebrow {
    margin: 0;
    width: fit-content;
    padding: 0.45rem 0.85rem;
    border: 1px solid color-mix(in srgb, var(--border) 80%, transparent);
    border-radius: 999px;
    background: color-mix(in srgb, var(--roast) 76%, transparent);
    color: var(--foam);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.76rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    max-width: 12ch;
    font-family: 'Playfair Display', serif;
    font-size: clamp(3rem, 8vw, 6.4rem);
    line-height: 0.93;
    letter-spacing: -0.04em;
  }

  .hero__lede {
    max-width: 60ch;
    margin: 0;
    color: var(--muted);
    font-size: clamp(1rem, 2vw, 1.15rem);
    line-height: 1.7;
  }

  .terminal {
    width: min(100%, 34rem);
    border: 1px solid var(--border);
    border-radius: 1.35rem;
    background: linear-gradient(180deg, color-mix(in srgb, var(--roast) 92%, transparent), color-mix(in srgb, var(--espresso) 80%, transparent));
    box-shadow: var(--shadow);
    overflow: hidden;
  }

  .terminal__header {
    display: flex;
    gap: 0.5rem;
    padding: 0.9rem 1rem;
    border-bottom: 1px solid color-mix(in srgb, var(--border) 80%, transparent);
    background: color-mix(in srgb, var(--espresso) 70%, transparent);
  }

  .terminal__header span {
    width: 0.7rem;
    height: 0.7rem;
    border-radius: 50%;
    background: color-mix(in srgb, var(--foam) 60%, var(--border));
  }

  .terminal__body {
    padding: 1.1rem 1.1rem 1.2rem;
    display: grid;
    gap: 0.55rem;
    font-family: 'JetBrains Mono', monospace;
  }

  .terminal__prompt,
  .terminal__hint {
    margin: 0;
    color: var(--muted);
    font-size: 0.88rem;
  }

  .terminal__line {
    margin: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    min-height: 2rem;
    font-weight: 600;
    color: var(--cream);
    font-size: clamp(1rem, 2vw, 1.1rem);
  }

  .terminal__cursor {
    animation: blink 1s step-end infinite;
    color: var(--caramel);
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3.1rem;
    padding: 0.85rem 1.35rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    font-weight: 700;
    letter-spacing: 0.02em;
    transition: transform 0.25s ease, background 0.25s ease, border-color 0.25s ease;
  }

  .button:hover {
    transform: translateY(-1px);
  }

  .button--primary {
    background: var(--caramel);
    color: var(--espresso);
    border-color: color-mix(in srgb, var(--caramel) 70%, white 10%);
  }

  .button--ghost {
    background: color-mix(in srgb, var(--roast) 78%, transparent);
    color: var(--cream);
  }

  .hero__aside {
    display: grid;
    gap: 1rem;
    justify-items: end;
  }

  .code-card {
    width: min(100%, 24rem);
    padding: 1rem 1rem 1.1rem;
    border: 1px solid var(--border);
    border-radius: 1.4rem;
    background: linear-gradient(180deg, color-mix(in srgb, var(--roast) 94%, transparent), color-mix(in srgb, var(--espresso) 86%, transparent));
    box-shadow: var(--shadow);
    animation: float 7s ease-in-out infinite;
    transition: opacity 0.4s ease, transform 0.4s ease;

    @starting-style {
      opacity: 0;
      transform: translateY(16px);
    }
  }

  .code-card__title {
    margin: 0 0 0.8rem;
    color: var(--foam);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.82rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .code-card pre {
    margin: 0;
    overflow-x: auto;
    color: var(--cream);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.88rem;
    line-height: 1.7;
  }

  .scroll-indicator {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    color: var(--muted);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .scroll-indicator i {
    position: relative;
    width: 2.2rem;
    height: 3.8rem;
    border: 1px solid var(--border);
    border-radius: 999px;
  }

  .scroll-indicator i::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 0.6rem;
    width: 0.45rem;
    height: 0.8rem;
    border-radius: 999px;
    background: var(--caramel);
    transform: translateX(-50%);
    animation: scroll 1.8s ease-in-out infinite;
  }

  @keyframes blink {
    50% {
      opacity: 0;
    }
  }

  @keyframes float {
    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes scroll {
    0%,
    100% {
      transform: translateX(-50%) translateY(0);
      opacity: 0.55;
    }

    50% {
      transform: translateX(-50%) translateY(1.45rem);
      opacity: 1;
    }
  }

  @media (min-width: 800px) {
    .hero {
      grid-template-columns: minmax(0, 1.2fr) minmax(280px, 0.8fr);
      gap: 2.5rem;
    }

    .hero__content {
      padding-right: 1rem;
    }
  }
</style>
